create table for_signup(
id bigint AUTO_INCREMENT PRIMARY KEY, 
num bigint NOT NULL,
u_name varchar(250) NOT NULL,
l_name varchar(250) NOT NULL, 
m_name varchar(250) NOT NULL,
g_name varchar(250) NOT NULL,
pass varchar(250) NOT NULL
);